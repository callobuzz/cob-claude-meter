node_modules/
dist/
*.js.map
*.tgz
.env
.env.*
!.env.example
.claude-meter/
.claude/

# npm auth lives here during a publish. Never commit it, even when it only
# holds a ${NPM_TOKEN} reference — the next person to edit it may paste the
# real token in.
.npmrc

# Browser-automation output from local verification runs.
.playwright-mcp/

# Internal planning docs (design/implementation plans)
docs/plans/
docs/prd.md
docs/working-ps.md
DEPLOY.md
docs/marketing/

# Local dashboard data: client assignments, tags and the scan cache.
# Ignored on purpose — it holds your client names and absolute project paths,
# and this repo is public. Back it up outside git (see README).
meter-data/
